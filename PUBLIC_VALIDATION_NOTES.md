# Validação Pública

Após o checkpoint `8ae60c2b`, o domínio público inicialmente exibiu a tela anterior de OAuth Manus. Após a sincronização de publicação, as rotas públicas foram validadas com sucesso: `/login` mostra o login de clientes por e-mail e senha e `/` mostra a landing comercial com os botões **Entrar**, **Criar conta**, **Começar agora** e **Acessar meu painel**.

Também foi confirmado que uma visita sem sessão a `/projects` retorna à landing pública e que o botão **Criar conta** abre corretamente o formulário `/register`.
