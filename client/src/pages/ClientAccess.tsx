import { useState } from "react";
import { ArrowLeft, LoaderCircle, LockKeyhole, Mail, ShieldCheck } from "lucide-react";
import { toast } from "sonner";
import { useLocation } from "wouter";
import { trpc } from "@/lib/trpc";
import { startLogin } from "@/const";

export default function ClientAccessPage({ mode }: { mode: "login" | "register" }) {
  const [, setLocation] = useLocation();
  const [name, setName] = useState(""); const [email, setEmail] = useState(""); const [password, setPassword] = useState("");
  const utils = trpc.useUtils();
  const login = trpc.auth.login.useMutation({ onSuccess: async ({ user }) => { utils.auth.me.setData(undefined, user); await utils.auth.me.invalidate(); toast.success("Login realizado."); setLocation("/"); }, onError: (error) => toast.error(error.message) });
  const register = trpc.auth.register.useMutation({ onSuccess: async ({ user }) => { utils.auth.me.setData(undefined, user); await utils.auth.me.invalidate(); toast.success("Conta criada. Bem-vindo à BuildForge."); setLocation("/"); }, onError: (error) => toast.error(error.message) });
  const isRegister = mode === "register"; const pending = login.isPending || register.isPending;
  return <main className="grid min-h-screen bg-slate-950 px-5 py-8 text-white lg:grid-cols-2"><section className="mx-auto flex w-full max-w-md flex-col justify-center"><button onClick={() => setLocation("/")} className="mb-10 inline-flex w-fit items-center gap-2 text-sm font-semibold text-slate-400 hover:text-white"><ArrowLeft className="h-4 w-4" />Voltar para apresentação</button><div className="rounded-3xl border border-white/10 bg-slate-900 p-7 shadow-2xl shadow-indigo-950/50"><span className="grid h-11 w-11 place-items-center rounded-2xl bg-violet-500 text-white"><LockKeyhole className="h-5 w-5" /></span><h1 className="mt-6 text-2xl font-semibold">{isRegister ? "Crie sua conta" : "Entre na sua conta"}</h1><p className="mt-2 text-sm leading-6 text-slate-400">{isRegister ? "Crie seu acesso de cliente para guardar projetos, releases e artefatos." : "Use seu e-mail e senha para acessar seus projetos e builds."}</p><form onSubmit={(event) => { event.preventDefault(); if (isRegister) register.mutate({ name, email, password }); else login.mutate({ email, password }); }} className="mt-6 space-y-4">{isRegister && <label className="block text-sm font-medium text-slate-300">Nome<input required minLength={2} value={name} onChange={(event) => setName(event.target.value)} className="mt-1.5 h-11 w-full rounded-xl border border-slate-700 bg-slate-950 px-3 text-sm outline-none focus:border-violet-500" placeholder="Seu nome ou empresa" /></label>}<label className="block text-sm font-medium text-slate-300">E-mail<div className="relative mt-1.5"><Mail className="pointer-events-none absolute left-3 top-3.5 h-4 w-4 text-slate-500" /><input required type="email" value={email} onChange={(event) => setEmail(event.target.value)} className="h-11 w-full rounded-xl border border-slate-700 bg-slate-950 pl-10 pr-3 text-sm outline-none focus:border-violet-500" placeholder="voce@empresa.com" /></div></label><label className="block text-sm font-medium text-slate-300">Senha<input required minLength={8} type="password" value={password} onChange={(event) => setPassword(event.target.value)} className="mt-1.5 h-11 w-full rounded-xl border border-slate-700 bg-slate-950 px-3 text-sm outline-none focus:border-violet-500" placeholder="Mínimo de 8 caracteres" /></label><button disabled={pending} className="inline-flex h-11 w-full items-center justify-center gap-2 rounded-xl bg-violet-600 text-sm font-bold hover:bg-violet-500 disabled:opacity-50">{pending && <LoaderCircle className="h-4 w-4 animate-spin" />}{isRegister ? "Criar conta e acessar" : "Entrar no painel"}</button></form><div className="mt-6 border-t border-white/10 pt-5 text-center text-sm text-slate-400">{isRegister ? "Já tem conta?" : "Ainda não tem conta?"}<button onClick={() => setLocation(isRegister ? "/login" : "/register")} className="ml-2 font-semibold text-violet-300 hover:text-violet-200">{isRegister ? "Entrar" : "Criar conta"}</button></div><div className="mt-5 rounded-xl border border-slate-700 bg-slate-950/60 p-3 text-xs leading-5 text-slate-400"><ShieldCheck className="mr-1 inline h-3.5 w-3.5 text-emerald-400" />A senha é protegida com hash e a sessão usa cookie seguro.</div><button onClick={() => startLogin()} className="mt-5 w-full text-xs font-semibold text-slate-500 underline hover:text-slate-300">Acesso administrativo com Manus</button></div></section><section className="hidden items-center justify-center p-10 lg:flex"><div className="max-w-md rounded-3xl border border-violet-400/15 bg-violet-500/10 p-9"><p className="text-xs font-semibold uppercase tracking-[.18em] text-violet-300">BuildForge para clientes</p><h2 className="mt-4 text-3xl font-semibold">Seu painel, seus builds, suas entregas.</h2><p className="mt-4 leading-7 text-slate-300">Após entrar, o cliente enxerga somente os próprios projetos, tokens, builds e downloads. Administradores mantêm acesso aos controles globais.</p></div></section></main>;
}
