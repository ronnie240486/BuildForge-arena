import { Headphones, LoaderCircle, Send } from "lucide-react";
import { useState } from "react";
import { toast } from "sonner";
import { trpc } from "@/lib/trpc";

export default function SupportPage() {
  const [subject, setSubject] = useState("");
  const [description, setDescription] = useState("");
  const [priority, setPriority] = useState<"low" | "normal" | "high" | "urgent">("normal");
  const utils = trpc.useUtils();
  const tickets = trpc.buildforge.support.list.useQuery();
  const create = trpc.buildforge.support.create.useMutation({ onSuccess: () => { toast.success("Ticket enviado para a central de suporte."); setSubject(""); setDescription(""); setPriority("normal"); void utils.buildforge.support.list.invalidate(); }, onError: (error) => toast.error(error.message) });
  return <div className="mx-auto max-w-6xl space-y-6 pb-8"><header><p className="text-xs font-bold uppercase tracking-[.18em] text-violet-500">Central comercial</p><h1 className="mt-2 text-3xl font-semibold tracking-tight text-slate-950 dark:text-white">Suporte</h1><p className="mt-2 text-sm text-slate-600 dark:text-slate-400">Registre dúvidas e problemas vinculados à operação da BuildForge.</p></header><section className="grid gap-5 lg:grid-cols-[.9fr_1.1fr]"><form onSubmit={(event) => { event.preventDefault(); create.mutate({ subject, description, priority }); }} className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm dark:border-slate-800 dark:bg-slate-950"><div className="flex items-center gap-3"><span className="rounded-xl bg-violet-500/10 p-2 text-violet-600 dark:text-violet-300"><Headphones className="h-5 w-5" /></span><div><h2 className="font-semibold text-slate-950 dark:text-white">Abrir ticket</h2><p className="text-xs text-slate-500">Descreva o necessário para que o atendimento possa ajudar.</p></div></div><label className="mt-5 block text-sm font-medium">Assunto<input required minLength={4} value={subject} onChange={(event) => setSubject(event.target.value)} className="mt-1.5 h-11 w-full rounded-xl border border-slate-300 bg-white px-3 text-sm dark:border-slate-700 dark:bg-slate-900" placeholder="Ex.: erro na build Android" /></label><label className="mt-4 block text-sm font-medium">Prioridade<select value={priority} onChange={(event) => setPriority(event.target.value as typeof priority)} className="mt-1.5 h-11 w-full rounded-xl border border-slate-300 bg-white px-3 text-sm dark:border-slate-700 dark:bg-slate-900"><option value="low">Baixa</option><option value="normal">Normal</option><option value="high">Alta</option><option value="urgent">Urgente</option></select></label><label className="mt-4 block text-sm font-medium">Descrição<textarea required minLength={12} value={description} onChange={(event) => setDescription(event.target.value)} className="mt-1.5 min-h-36 w-full rounded-xl border border-slate-300 bg-white p-3 text-sm dark:border-slate-700 dark:bg-slate-900" placeholder="Informe o projeto, o que aconteceu e o resultado esperado…" /></label><button disabled={create.isPending} className="mt-5 inline-flex h-11 items-center gap-2 rounded-xl bg-violet-600 px-4 text-sm font-semibold text-white disabled:opacity-50">{create.isPending ? <LoaderCircle className="h-4 w-4 animate-spin" /> : <Send className="h-4 w-4" />}Enviar ticket</button></form><section className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm dark:border-slate-800 dark:bg-slate-950"><h2 className="font-semibold text-slate-950 dark:text-white">Seus tickets</h2><div className="mt-4 space-y-3">{tickets.isLoading ? <LoaderCircle className="h-5 w-5 animate-spin text-violet-500" /> : tickets.data?.length ? tickets.data.map((ticket) => <article key={ticket.id} className="rounded-xl border border-slate-200 p-4 dark:border-slate-800"><div className="flex items-start justify-between gap-3"><div><p className="text-sm font-semibold text-slate-950 dark:text-white">{ticket.subject}</p><p className="mt-1 text-xs text-slate-500">Prioridade {ticket.priority} · {ticket.ticketStatus.replace("_", " ")}</p></div><span className="rounded-full bg-violet-500/10 px-2 py-1 text-xs font-semibold text-violet-700 dark:text-violet-300">#{ticket.id}</span></div><p className="mt-3 line-clamp-3 text-sm text-slate-600 dark:text-slate-400">{ticket.description}</p></article>) : <p className="rounded-xl bg-slate-50 p-5 text-sm text-slate-500 dark:bg-slate-900">Nenhum ticket aberto.</p>}</div></section></section></div>;
}
