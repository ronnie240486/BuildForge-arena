import { useState } from "react";
import { Bot, FileCode2, LoaderCircle, Route, Sparkles } from "lucide-react";
import { Streamdown } from "streamdown";
import { toast } from "sonner";
import { trpc } from "@/lib/trpc";
import { useLocation } from "wouter";

type Framework = "android" | "flutter" | "react_native";

export default function StudioPage() {
  const [, setLocation] = useLocation();
  const [tab, setTab] = useState<"generate" | "migrate">("generate");
  const [name, setName] = useState("");
  const [framework, setFramework] = useState<Framework>("flutter");
  const [prompt, setPrompt] = useState("");
  const [sourceDescription, setSourceDescription] = useState("");
  const [plan, setPlan] = useState<string>("");
  const generate = trpc.buildforge.studio.generateApp.useMutation({ onSuccess: (result) => { toast.success(`Projeto inicial criado com ${result.files.length} arquivo(s).`); setLocation("/projects"); }, onError: (error) => toast.error(error.message) });
  const migrate = trpc.buildforge.studio.planMigration.useMutation({ onSuccess: (result) => { setPlan(result.plan); toast.success(`Plano criado por ${result.model}.`); }, onError: (error) => toast.error(error.message) });
  return <div className="mx-auto max-w-6xl space-y-6 pb-8"><header><p className="text-xs font-semibold uppercase tracking-[0.18em] text-violet-500">IA restaurada</p><h1 className="mt-2 text-3xl font-semibold tracking-tight text-slate-950 dark:text-white">Studio de aplicativos</h1><p className="mt-2 max-w-3xl text-sm leading-6 text-slate-600 dark:text-slate-400">Transforme uma ideia em um projeto inicial compactado ou receba um plano técnico para migrar código existente. Todo resultado é revisável antes de entrar na fila de build.</p></header><div className="flex rounded-xl border border-slate-200 bg-white p-1 dark:border-slate-800 dark:bg-slate-950"><button onClick={() => setTab("generate")} className={`flex-1 rounded-lg px-3 py-2 text-sm font-semibold ${tab === "generate" ? "bg-violet-600 text-white" : "text-slate-500"}`}>Gerar aplicativo</button><button onClick={() => setTab("migrate")} className={`flex-1 rounded-lg px-3 py-2 text-sm font-semibold ${tab === "migrate" ? "bg-violet-600 text-white" : "text-slate-500"}`}>Planejar migração</button></div>{tab === "generate" ? <section className="grid gap-5 lg:grid-cols-[.95fr_1.05fr]"><form onSubmit={(event) => { event.preventDefault(); generate.mutate({ name, framework, prompt }); }} className="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm dark:border-slate-800 dark:bg-slate-950"><div className="flex items-center gap-3"><span className="rounded-xl bg-violet-500/10 p-2 text-violet-600 dark:text-violet-300"><Sparkles className="h-5 w-5" /></span><div><h2 className="font-semibold text-slate-950 dark:text-white">Brief do aplicativo</h2><p className="text-xs text-slate-500">Gera um ZIP de fonte inicial seguro e editável.</p></div></div><label className="mt-5 block text-sm font-medium text-slate-700 dark:text-slate-300">Nome<input required value={name} onChange={(event) => setName(event.target.value)} placeholder="Minha loja" className="mt-1.5 h-11 w-full rounded-xl border border-slate-300 bg-white px-3 text-sm dark:border-slate-700 dark:bg-slate-900" /></label><label className="mt-4 block text-sm font-medium text-slate-700 dark:text-slate-300">Stack<select value={framework} onChange={(event) => setFramework(event.target.value as Framework)} className="mt-1.5 h-11 w-full rounded-xl border border-slate-300 bg-white px-3 text-sm dark:border-slate-700 dark:bg-slate-900"><option value="flutter">Flutter</option><option value="android">Android nativo</option><option value="react_native">React Native</option></select></label><label className="mt-4 block text-sm font-medium text-slate-700 dark:text-slate-300">O que o aplicativo deve fazer?<textarea required minLength={12} value={prompt} onChange={(event) => setPrompt(event.target.value)} placeholder="Catálogo de produtos com lista, detalhes, favoritos e cores da marca…" className="mt-1.5 min-h-36 w-full rounded-xl border border-slate-300 bg-white p-3 text-sm dark:border-slate-700 dark:bg-slate-900" /></label><button disabled={generate.isPending} className="mt-5 inline-flex h-11 items-center gap-2 rounded-xl bg-violet-600 px-4 text-sm font-semibold text-white disabled:opacity-50">{generate.isPending ? <LoaderCircle className="h-4 w-4 animate-spin" /> : <FileCode2 className="h-4 w-4" />}Gerar projeto inicial</button></form><article className="rounded-2xl border border-violet-200 bg-violet-50/70 p-6 dark:border-violet-900/60 dark:bg-violet-500/10"><Bot className="h-6 w-6 text-violet-600 dark:text-violet-300" /><h2 className="mt-4 font-semibold text-violet-950 dark:text-violet-100">Como funciona</h2><ol className="mt-3 space-y-3 text-sm leading-6 text-violet-900/80 dark:text-violet-100/75"><li>1. O modelo gera uma estrutura mínima com arquivos de fonte, sem binários ou segredos.</li><li>2. A plataforma armazena o ZIP como fonte do novo projeto.</li><li>3. Você revisa, conecta um worker e cria uma release quando quiser.</li><li>4. A IA pode continuar ajudando caso o build apresente falhas.</li></ol></article></section> : <section className="grid gap-5 lg:grid-cols-[.95fr_1.05fr]"><form onSubmit={(event) => { event.preventDefault(); migrate.mutate({ target: framework, sourceDescription }); }} className="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm dark:border-slate-800 dark:bg-slate-950"><div className="flex items-center gap-3"><span className="rounded-xl bg-indigo-500/10 p-2 text-indigo-600 dark:text-indigo-300"><Route className="h-5 w-5" /></span><div><h2 className="font-semibold text-slate-950 dark:text-white">Planejador de migração</h2><p className="text-xs text-slate-500">Analisa o contexto e propõe etapas revisáveis.</p></div></div><label className="mt-5 block text-sm font-medium text-slate-700 dark:text-slate-300">Destino<select value={framework} onChange={(event) => setFramework(event.target.value as Framework)} className="mt-1.5 h-11 w-full rounded-xl border border-slate-300 bg-white px-3 text-sm dark:border-slate-700 dark:bg-slate-900"><option value="flutter">Flutter</option><option value="android">Android nativo</option><option value="react_native">React Native</option></select></label><label className="mt-4 block text-sm font-medium text-slate-700 dark:text-slate-300">Contexto do código atual<textarea required minLength={12} value={sourceDescription} onChange={(event) => setSourceDescription(event.target.value)} placeholder="Aplicativo atual usa WebView, login próprio, API REST e precisa migrar telas de catálogo…" className="mt-1.5 min-h-44 w-full rounded-xl border border-slate-300 bg-white p-3 text-sm dark:border-slate-700 dark:bg-slate-900" /></label><button disabled={migrate.isPending} className="mt-5 inline-flex h-11 items-center gap-2 rounded-xl bg-indigo-600 px-4 text-sm font-semibold text-white disabled:opacity-50">{migrate.isPending && <LoaderCircle className="h-4 w-4 animate-spin" />}Criar plano técnico</button></form><article className="min-h-80 rounded-2xl border border-slate-200 bg-white p-6 shadow-sm dark:border-slate-800 dark:bg-slate-950">{plan ? <Streamdown>{plan}</Streamdown> : <div className="grid min-h-64 place-items-center text-center"><div><Route className="mx-auto h-9 w-9 text-indigo-300" /><p className="mt-3 font-semibold text-slate-700 dark:text-slate-200">Seu plano aparecerá aqui</p><p className="mt-1 text-sm text-slate-500">Descreva o projeto atual e escolha a stack de destino.</p></div></div>}</article></section>}</div>;
}
